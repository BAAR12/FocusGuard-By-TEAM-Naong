<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Guard" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Force Light Mode to fix system components on dark backgrounds -->
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>

    <!-- Dark theme for specific activities like Login -->
    <style name="Theme.Guard.Dark" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>

    <!--
    NEW DIALOG THEME FOR PIN CHECK
    This makes the activity a floating pop-up.
    -->
    <style name="Theme.Guard.Dialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowCloseOnTouchOutside">false</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!-- Style for the Spinner dropdown -->
    <style name="SpinnerDropdownTheme" parent="Widget.AppCompat.DropDownItem.Spinner">
        <item name="android:background">@color/white</item>
        <item name="android:textColor">@color/dark_text</item>
    </style>

    <!-- Style for our custom dialogs -->
    <style name="AlertDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@color/white</item>
        <item name="android:textColorPrimary">@color/dark_text</item>
        <item name="colorAccent">@color/link_blue</item>

    </style>

</resources>